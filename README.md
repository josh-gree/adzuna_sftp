# To run

- populate `.env.sample` with credentials and rename to `.env`

```
> pip install pipenv
> pipenv install
> pipenv run python adzuna_sftp.py
```

## Expected output

```
[]
['sample.csv']
[]
```
